# LimitRange: default requests and limits for pods in payments namespace
apiVersion: v1
kind: LimitRange
metadata:
  name: payments-limitrange
  namespace: payments
spec:
  limits:
  - default:
      cpu: "100m"
      memory: "64Mi"
    defaultRequest:
      cpu: "50m"
      memory: "32Mi"
    type: Container
