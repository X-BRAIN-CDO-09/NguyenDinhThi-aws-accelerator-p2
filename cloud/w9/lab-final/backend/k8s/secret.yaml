# ============================================================
# Secret: backend-secret
# Sync Wave 0 — tạo trước Rollout (wave 1)
# DB credentials dạng base64 (placeholder an toàn)
# KHÔNG commit secret thật lên Git — dùng Sealed Secrets hoặc Vault ở production
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: demo
  annotations:
    argocd.argoproj.io/sync-wave: "0"
type: Opaque
data:
  # echo -n "localhost" | base64
  DB_HOST: bG9jYWxob3N0
  # echo -n "demo_db" | base64
  DB_NAME: ZGVtb19kYg==
  # echo -n "demo_password_changeme" | base64
  DB_PASSWORD: ZGVtb19wYXNzd29yZF9jaGFuZ2VtZQ==
  # echo -n "demo_user" | base64
  DB_USER: ZGVtb191c2Vy
